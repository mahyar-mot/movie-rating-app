version: "3.7"

services:
    backend:
        build:
            context: ./backend
        ports:
            - "8000:8000"
        volumes: 
            - "./backend:/app"
        command: >
         sh -c "python manage.py migrate &&
                python manage.py runserver 0.0.0.0:8000"
